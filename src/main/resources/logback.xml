<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="true" scan="true" scanPeriod="10 seconds">
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />

    <property name="CONSOLE_LOG_PATTERN_HORA" value="%clr(%d{yyyy-MM-dd E HH:mm:ss.SSS}){blue} %clr(%-5p) %clr(${PID}){faint}" />

    <property name="CONSOLE_LOG_PATTERN" value="${CONSOLE_LOG_PATTERN_HORA} %clr(---){faint} %clr([%8.30t]){cyan} %clr(%-30.30logger{0}){blue} %clr(:){red} %clr(%m){faint}%n" />

    <logger name="com.zaxxer.hikari">
        <level value="DEBUG" />
    </logger>

    <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <root level="INFO">
        <!--<appender-ref ref="LOG_FILE_PLAIN" />-->
        <appender-ref ref="CONSOLE_JSON" />
    </root>
</configuration>
